import React from 'react'
import Layout from '../components/layout'

export default function About() {
  return (
    <Layout>
    <div>
        <h1>About Page</h1>
        <p>gatsby will create a route with the file name. So we can navigate to the pages.</p>
        <p>gatsby will create a route with the file name. So we can navigate to the pages.</p>
        <p>gatsby will create a route with the file name. So we can navigate to the pages.</p>
        <p>gatsby will create a route with the file name. So we can navigate to the pages.</p>
        
    </div>
    </Layout>
  )
}
